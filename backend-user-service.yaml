apiVersion: v1
kind: Service
metadata:
  name: udagram-user-app
  labels:
    run: udagram-user-app
spec:
  ports:
  - port: 80
    protocol: TCP
  selector:
    run: udagram-user-app
      - containerPort: 8080