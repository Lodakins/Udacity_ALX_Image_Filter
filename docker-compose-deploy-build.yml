version: "3"
services:
  reverseproxy:
    build:
      context: ./udagram-reverseproxy
    image: lodakins/reverseproxy
  backend_user:
    build:
      context: ./udagram-api-user
    image: lodakins/udagram-api-user
  backend_feed:
    build:
      context: ./udagram-api-feed
    image: lodakins/udagram-api-feed
  frontend:
    build:
      context: ./udagram-frontend
    image: lodakins/udagram-frontend:local